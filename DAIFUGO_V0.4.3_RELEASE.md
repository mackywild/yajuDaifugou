# Daifugo v0.4.3 — J-BACK UPDATE

v0.4.3は、野獣上がり仕様の修正と、新ローカルルール「Jバック」を追加するアップデートです。

## 主要変更

### 野獣上がり仕様修正

野獣上がりは `8×1 + 10×N（N>=1）` の最終手札で成立します。

```text
8,10          → 8 → 10
8,10,10       → 8 → 10,10
8,10,10,10    → 8 → 10,10,10
8,10,10,10,10 → 8 → 10,10,10,10
```

8は単体で提出し、8切り後に残っている10を全てまとめて提出して上がります。

### Jバック

Jを含む合法手を提出すると、場が流れるまでJバック状態になります。

- 通常状態 + Jバック: 強弱反転。3が最強
- 革命 + Jバック: 革命の反転を相殺。2が最強
- 実効強弱は `revolution XOR jackBack`
- 場が流れるとJバック解除

Jバック発動時は全端末へ `JACK_BACK` イベントを配信し、「バック気持ちいい」カットインを表示します。

### 早漏

通常状態のJバック直後、実際の次手番プレイヤーが単体3を提出した場合に `EARLY_SHOT` イベントを発行します。

- カットイン: 「早漏 / 早すぎるッ！」
- 次プレイヤーがPASSした場合は判定終了
- 革命 + Jバックでは3が最強ではないため早漏判定対象外
- 3のペア/トリプル/4枚組は対象外

## クライアント

- Web: Jバック設定、状態表示、Jバック/早漏カットイン
- Android: Jバック設定、状態ピル、全画面カットイン、オフラインCPU戦対応
- iOS: Jバック設定、状態表示、カットイン

## CPU

CPU合法手生成は `GameState.isStrengthReversed()` を使用します。
N-GODの特徴量 `f[7]` も実効強弱反転状態へ変更しました。Jバック導入前は `revolution == strengthReversed` のため、既存学習済み重みとの入力意味の互換性を維持します。

## Version

- Android versionName: `0.4.3`
- Android versionCode: `7`
- iOS MARKETING_VERSION: `0.4.3`
- iOS CURRENT_PROJECT_VERSION: `7`

## Hotfix: スペード3返し

JOKERをスペード3で返した場合、そのスペード3は通常の「3」ではなく、そのトリックにおける絶対最強札として扱います。
そのため次のプレイヤー以降は他のカードを出せず、PASSのみ可能です。全員がPASSして場が流れた後は、スペード3を出したプレイヤーが次のトリックを開始します。

通常出しのスペード3はこの特別状態にはならず、通常の3として比較されます。
