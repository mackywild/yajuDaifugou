# v0.4.4 Review Report

## レビュー観点

1. 仕様どおりの設計になっているか
2. 問題なく起動できるか
3. 命名規則などが統一されているか
4. レビュアーが気になった指摘事項がないか

## 1. 仕様適合

**PASS**

- オンライン: 2〜8人
- CPU: 人間1 + CPU1〜7人
- 8人配牌、手番、特殊ルール、順位1〜8を確認
- 9人目はRoom/GameStateの両層で拒否

## 2. 起動・実行性

**PASS WITH CI GATE**

- Java compile PASS
- 121 tests PASS / 0 FAIL
- AndroidローカルCPU7人起動 PASS
- Web JS / Swift parse PASS
- Gradle配布サーバーへのDNS制限によりローカルAPKビルドのみ未実行

## 3. 命名・統一

**PASS**

レビュー時に人数定数が複数クラスへ分散する設計を避けるため、`GameLimits`へ`MIN_PLAYER_COUNT` / `MAX_PLAYER_COUNT` / `MAX_CPU_COUNT`を集約した。Androidゲームコアも同じ定数を参照する。

## 4. レビュー指摘と対応

### R-01 Android CPU人数ボタンが7個固定Rowでは狭い
- 判定: BLOCKING UI
- 対応: `LazyRow`へ変更。
- 結果: FIXED

### R-02 CPUプレイモーションが3人分の開始位置しか持たない
- 判定: BLOCKING UX
- 対応: 相手人数とindexから-1.0〜+1.0へ正規化し、最大7人を連続配置。
- 結果: FIXED

### R-03 iOSの相手7人をHStackへ押し込むと可読性が落ちる
- 判定: BLOCKING UI
- 対応: 水平ScrollView + 122pt固定幅へ変更。
- 結果: FIXED

### R-04 8人配牌テストでJoker2枚をdistinct 2枚として扱っていた
- 判定: TEST DEFECT
- 対応: Joker枚数=2、通常52枚distinctとして検証するよう修正。
- 結果: FIXED

### R-05 N-GODの自己対戦データ生成が2〜4人のまま
- 判定: NON-BLOCKING MODEL QUALITY
- 対応: Generatorを2〜8人へ拡張。現行重みの再学習は行わず、8人戦で合法手・完走性を検証。
- 結果: FUNCTIONAL FIXED / STRENGTH RE-BENCHMARK PENDING

## 最終判定

**APPROVED WITH CI GATE**

ブロッキング指摘はすべて修正済み。GitHub ActionsでServer TestとAndroid APK buildが通過すればリリース可能。
