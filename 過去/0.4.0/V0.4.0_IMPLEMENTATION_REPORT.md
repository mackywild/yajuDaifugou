# Daifugo v0.4.0 実装レポート

## 実装テーマ

**CPU BATTLE UPDATE — 【ひとりでイク】**

## 実装済み

- メインメニュー
- 既存部屋対戦を「マルチプレイ」へ整理
- CPU戦【ひとりでイク】
- CPU人数1〜3人
- 簡単 / 普通 / 難しい / N-GOD
- CPU対戦ごとの特殊ルール設定
- Web / Android / iOSのCPU戦導線
- サーバーauthoritativeなCPU自動進行
- CPUプレイヤー / 難易度のレスポンス表示
- N-GOD自己対戦学習基盤
- N-GOD学習済み重みのJava組み込み
- CPU難易度ベンチマーク
- CPU専用JUnit追加

## N-GOD

学習データ:

```text
self-play games : 5,000
samples         : 278,188
features        : 38
train           : 250,369
validation      : 27,819
seed            : 8101919
```

最終epoch:

```text
train_mse=0.145395
val_mse=0.145951
val_mae=0.331530
corr=0.3692
```

## 再検証

この成果物生成時に実施:

```text
Java 21 core + CPU compile       PASS
CPU feature smoke               PASS
JUnit/core test source compile  PASS
Web JavaScript syntax           PASS
Swift Models/API typecheck      PASS
Random-setting self-play 300    PASS (16,840 samples, deadlock 0)
```

4,000ゲーム難易度ベンチマーク:

```text
EASY   winRate=0.053 avgRank=3.309 dead=0
NORMAL winRate=0.230 avgRank=2.503 dead=0
HARD   winRate=0.259 avgRank=2.485 dead=0
N-GOD  winRate=0.313 avgRank=2.173 dead=0
```

## 未実行項目

実行環境が`services.gradle.org`を名前解決できないため、この環境内ではGradle distributionを取得できず、`./gradlew test` / APK / Xcode simulator buildは未実行。

GitHub ActionsのServer / Android / iOS workflowで最終ビルドを行う。
