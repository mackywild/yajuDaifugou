# 野獣ルール音声

ここへ友達のアフレコ音声を置くだけです。

- `yaju_available.mp3` / `.wav` / `.m4a` — 「野獣上がりできます」
- `yaju_success.mp3` / `.wav` / `.m4a` — 「やりますねぇ」

GitHub Actions / XcodeGen はこの `AudioResources` ディレクトリをリソースとして取り込みます。
音声が無い場合は無音でゲームを継続します。
