# CPU写真アイコン対応 レビュー結果

## 1. 仕様どおりの設計か
PASS

- CPUローカル対戦のみ写真を使用する。
- CPU 1〜7へ `cpu_01.jpg`〜`cpu_07.jpg` を一意に割り当てる。
- 対戦画面、Jバック/早漏カットイン、野獣カットイン、リザルトで同じ写真を使用する。
- 写真未配置・読込失敗時は従来の生成アイコンへフォールバックする。
- 通信対戦のプレイヤーアイコンには影響しない。

## 2. 問題なく起動できるか
CONDITIONAL PASS

- Kotlinソースの構文チェックで構文エラーなし。
- 写真が0枚でもフォールバックするため、写真未配置が起動失敗要因にならない設計。
- この環境ではGradle配布サーバーを名前解決できず、Android実Gradleビルドは未実行。GitHub Actionsを最終CIゲートとする。

## 3. 命名規則などが統一されているか
PASS

- 配置ディレクトリ: `cpu_avatars`
- ファイル: `cpu_01.jpg`〜`cpu_07.jpg`
- Kotlin: `cpuAvatarAssetName`, `decodeCpuAvatar`, `CPU_AVATAR_DECODE_EDGE_PX`

## 4. その他指摘事項
レビュー時に以下を改善済み。

- 大きなカメラ写真をそのままデコードするとメモリ負荷が高い → 最大辺512px相当へサンプリングして読込。
- ComposeのUIスレッドで画像デコードするとカクつく可能性 → `Dispatchers.IO`で非同期読込。
- 写真が壊れている/存在しない場合にクラッシュする可能性 → `runCatching` + 生成アイコンへのフォールバック。

非ブロッキング事項:

- 写真のEXIF向きは事前に確定しておくことを推奨。
- 公開APKへ実在人物の写真を同梱する場合、利用許諾とEXIF/GPS情報を確認すること。
